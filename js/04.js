let resultado;
resultado = Math.PI;
resultado = Math.round(2.5);
resultado = Math.ceil(2.2); //redondea siempre hacia arriba
resultado = Math.floor(2.9); // redondea siempre hacia abajo
resultado = Math.sqrt(144); // raíz cuadrada
resultado = Math.abs(-34); // convierte un número negativo en positivo
resultado = Math.min(2, 7, 8, 4); // nos devuelve el número más bajo
resultado = Math.max(2, 7, 8, 4); // nos devuelve el número más alto
resultado = Math.random(); // nos devuelve el número aleatorio
resultado = Math.floor(Math.random() * 30); // conjunto

console.log(resultado);
